import { HomePage} from 'components/HomePage/HomePage';
import { AppBox } from 'components/Common/Theme';

export const Layout = () => {
  return (
    <AppBox>
      <HomePage />
    </AppBox>
  );
};
